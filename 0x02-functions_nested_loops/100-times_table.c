#include "main.h"

void print_times_table(int n)
{
    int i = 0;
    int j = 0;

    if (n == 15 || n < 0)
        return;
    for (; i < n; i++)
    {
        for (; j < n; j++)
        _putchar(i)
    }
}
